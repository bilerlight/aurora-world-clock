<Window x:Class="AuroraClock.UsageWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CC Switch Usage"
        Width="240" Height="170"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True">

    <Grid x:Name="Root" Background="Transparent">
        <Border x:Name="Glass" CornerRadius="22" Background="Transparent">
            <Grid>
                <Border x:Name="Backdrop" CornerRadius="22" IsHitTestVisible="False">
                    <Border.Background>
                        <ImageBrush x:Name="BackdropBrush" Stretch="Fill" />
                    </Border.Background>
                </Border>
                <Border x:Name="Tint" CornerRadius="22" IsHitTestVisible="False" Background="#8C0A0F18" />
                <Border x:Name="Sheen" CornerRadius="22" IsHitTestVisible="False">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0.4,1">
                            <GradientStop Color="#28FFFFFF" Offset="0" />
                            <GradientStop Color="#00000000" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Border x:Name="Rim" CornerRadius="22" IsHitTestVisible="False"
                        BorderThickness="1" BorderBrush="#2EFFFFFF" />

                <Grid Margin="16,13,16,13">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="LiveDot" Width="8" Height="8" Fill="#FF57E08A"
                                     VerticalAlignment="Center" Margin="0,0,7,0" />
                            <TextBlock Text="CC Switch 用量" FontFamily="Segoe UI Variable Display, Segoe UI"
                                       FontWeight="SemiBold" FontSize="13" Foreground="#F2F6FC"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock x:Name="UpdatedText" HorizontalAlignment="Right" VerticalAlignment="Center"
                                   FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="10.5"
                                   Foreground="#8FA6C2E0" />
                    </Grid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock x:Name="CostText" Text="$0.00"
                                   FontFamily="Segoe UI Variable Display, Segoe UI" FontWeight="SemiBold"
                                   FontSize="30" Foreground="#F2F6FC" />
                        <TextBlock x:Name="ReqText" Text=""
                                   FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="11"
                                   Foreground="#8FA6C2E0" VerticalAlignment="Bottom"
                                   Margin="8,0,0,6" />
                    </StackPanel>

                    <TextBlock x:Name="TokenText" Grid.Row="2" Text=""
                               FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="11.5"
                               Foreground="#9BB0C4E0" Margin="0,1,0,8" />

                    <StackPanel x:Name="Bars" Grid.Row="3" VerticalAlignment="Bottom" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
